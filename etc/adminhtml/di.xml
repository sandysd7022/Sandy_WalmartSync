<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">
    <!--
        UI data-source collection registrations belong in the global etc/di.xml.
        Registering the collections array at the adminhtml area stage replaces
        Magento's core map and can remove sales_order_grid_data_source.
    -->
</config>
